{
  "name": "secure-cicd-demo-app",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal demo app for the Secure CI/CD Platform pipeline (build -> Trivy scan -> ECR -> ECS Fargate).",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
